version: "3"
services:
    core:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./dotcore:/root/.core
            - ./utils:/utils
            - ./src:/root/src
            - ./results:/results
        privileged: true
        env_file:
            - experiment_settings.env
        environment:
            - DISPLAY